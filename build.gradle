// 根目录 build.gradle (最终修正版)
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 这是告诉 Gradle 怎么编译安卓项目的核心插件
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}

// 保持这里为空，不要写 allprojects { ... }