// 根目录 build.gradle (极简版)
buildscript {
    dependencies {
        // 保持这个版本即可
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}

// 删掉原来的 allprojects { repositories { ... } } 块
// 因为已经在 settings.gradle 里统一配置过了